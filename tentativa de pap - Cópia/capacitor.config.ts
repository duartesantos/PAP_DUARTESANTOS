import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AppTurismo_Carla',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
